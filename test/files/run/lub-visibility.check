
scala> // should infer List[scala.collection.immutable.Seq[Nothing]]

scala> // but reverted that for SI-5534.

scala> val x = List(List(), Vector())
x: List[collection.immutable.Seq[Nothing] with collection.AbstractSeq[Nothing] with java.io.Serializable] = List(List(), Vector())

scala> :quit
